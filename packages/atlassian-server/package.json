{
  "name": "atlassian-server",
  "version": "0.1.0",
  "description": "MCP server for private/self-hosted Atlassian Jira and Confluence instances (Data Center/Server)",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "atlassian-mcp-private": "dist/index.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "dev": "tsx src/index.ts",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "lint": "eslint src --ext .ts",
    "lint:fix": "eslint src --ext .ts --fix",
    "typecheck": "tsc --noEmit",
    "format": "prettier --check src test",
    "format:fix": "prettier --write src test",
    "security:audit": "npm audit --audit-level=moderate",
    "validate": "npm run typecheck && npm run lint && npm test && npm run security:audit",
    "ci": "npm run typecheck && npm run lint && npm run format && npm run test:coverage && npm run security:audit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "@vitest/coverage-v8": "^2.0.0",
    "eslint": "^9.0.0",
    "eslint-plugin-security": "^3.0.0",
    "prettier": "^3.0.0",
    "tsup": "^8.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0",
    "vitest": "^2.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
